spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
            scope:
              - openid
              - profile
              - email


cloud:
  aws:
    region: ap-northeast-2

  s3:
    bucket: springboot-developer-neon1005