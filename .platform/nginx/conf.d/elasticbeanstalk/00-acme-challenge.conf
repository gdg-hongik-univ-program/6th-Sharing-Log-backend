# Keep the HTTP-01 challenge reachable before the postdeploy hook creates the
# host-specific HTTP redirect and HTTPS server blocks.
location ^~ /.well-known/acme-challenge/ {
    root /var/lib/letsencrypt;
    default_type text/plain;
    try_files $uri =404;
}
